curl -s get.gvmtool.net | bash

Please open a new terminal, or run the following in the existing one:

    source "/Users/andrew/.gvm/bin/gvm-init.sh"

Then issue the following command:

    gvm help


gvm install groovy
gvm install grails
gvm list gradle


gvm config: .gvm/etc/config
