"how does meteor do this stuff?"
- server
    - livequery (real-time mongoDB)
- communication
    - DDP (distributed data protocol)
- client
    - minimongo (client-side cache)
    - tracker (dependency tracking)
    - blaze (reactive html rendering)

ddp
- simple, human-readable protocol for remote procedure calls and data synchronization

ddp-analyzer-proxy

rpc uses Meteor.methods()

    Meteor.methods({
        someMethod: function(arguments..) {
        }
    });

nice:
- can put DDP in front of api's/other live sources
