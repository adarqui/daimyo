the four components:
- app structure
- packages
- server architecture
- security*

app structure:
/client
    /css
    /js (third party)
    /lib
        /helpers
        /meteor
            router.js, startup.js, subscriptions.js
    /views
        /view-group
            template.html, template.js
/lib
    /helpers
    /models
        init.js, individual model files (UserModel.js ..)
/packages
/private
/public
    /img
    /fonts
    /js
/server
    /api (REST API - optional)
    /config
        accounts.js
        users.js
    /helpers
    /lib
        settings.js
        packages.js
        startup.js
    /models
    /publish
/tests


server architecture:
- meteor-cluster : package for fast data synchronization between server instances

meteor servers only talk to each other every 10 seconds
- solution: meteor-oplog branch?

mongodb is the bottleneck
